openai>=1.66.0
requests
httpx
dulwich
starlette
python-multipart
uvicorn[standard]
websockets
huggingface_hub
PyYAML>=6.0
pytest>=7.0
claude-agent-sdk>=0.1.60
# A2A (Agent-to-Agent) protocol is part of the packaged/base runtime.
a2a-sdk[http-server]>=0.3.20,<1.0.0
# optional: local model server (requires Metal-enabled build on macOS)
# CMAKE_ARGS="-DGGML_METAL=on" pip install llama-cpp-python[server]
